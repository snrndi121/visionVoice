#ifndef _USER_TYPE_HPP_
#define _USER_TYPE_HPP_

//def-type
typedef vector < string > vec_str;
typedef vector < uint > vec_uint;
typedef pair < string, string > pair_str;
typedef pair < string, float > pair_score;//(문자열 정보, 스코어)
typedef vector < pair_str > vec_pair_str;
typedef vector < vec_uint > vec_vec_uint;
typedef vector < pair_score > vec_pair_score;

#endif
